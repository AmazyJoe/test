<%= form_with(model: search_history) do |form| %>
  <% if search_history.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(search_history.errors.count, "error") %> prohibited this search_history from being saved:</h2>

      <ul>
        <% search_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :query, style: "display: block" %>
    <%= form.text_field :query %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
